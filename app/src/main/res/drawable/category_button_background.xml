<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/category_button_background.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/> <!-- Background color for normal state -->
            <corners android:radius="16dp"/> <!-- Rounded corners, adjust as needed -->
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/white"/> <!-- Background color for pressed state -->
            <corners android:radius="16dp"/> <!-- Rounded corners, adjust as needed -->
        </shape>
    </item>
</selector>